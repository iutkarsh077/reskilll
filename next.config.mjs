/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            'media4.giphy.com', 'images.unsplash.com', "th.bing.com"
        ]
    }
};

export default nextConfig;
